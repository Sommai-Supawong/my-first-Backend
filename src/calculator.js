// import {add, divis, multi, sub, mod} from "./math.js";
import * as Math from "./math.js";

const a = 5454515;
const b = 31151;

console.log(`${a} + ${b} =  ${Math.add(a, b)}`);
console.log(`${a} - ${b} =  ${Math.sub(a, b)}`);
console.log(`${a} * ${b} =  ${Math.multi(a, b)}`);
console.log(`${a} / ${b} =  ${Math.divis(a, b)}`);
console.log(`${a} % ${b} =  ${Math.mod(a, b)}`);